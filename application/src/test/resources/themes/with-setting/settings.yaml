apiVersion: v1alpha1
kind: Setting
metadata:
  name: default-setting
spec:
  forms:
    - group: basic
      label: Basic
      formSchema:
        - $formkit: text
          name: email
          value: example@example.com
