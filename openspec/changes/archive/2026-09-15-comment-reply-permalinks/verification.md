## Verification — 2026-09-15

### Automated checks

- The initial implementation passed 123 focused backend tests, covering permalink formatting and enrichment, comment/reply reads, content update compatibility, notification publishers, and actual default-template rendering.
- `CommentPermalinkIntegrationTest` uses a random-port Halo server, real extension storage, HTTP, Basic authentication, and existing role bindings. Its 16 cases cover both visibility levels, anonymous/owner/unrelated/moderator access, hidden-thread ownership, deleted/missing/wrong-parent targets, sanitization, counters, cache headers, shared response URLs, missing/changing subjects, one subject lookup per reply page, and preservation of configured absolute permalinks.
- `./gradlew spotlessCheck`, `pnpm -C ui typecheck`, and `pnpm -C ui lint` passed. UI commands used the declared pnpm 11.17.0 via `npx` because the shell's default pnpm differed.
- OpenAPI and client files were generated by `./gradlew generateOpenApiDocs` and `pnpm -C ui api-client:gen`; the client package was rebuilt before checking Console. Generator-added trailing whitespace was normalized automatically with `git apply --whitespace=fix` on the generated diff.

The initial focused backend command selected `CommentPermalink*Test`, `CommentServiceImplTest`, `CommentContentUpdateTest`, `CommentNotificationReasonPublisherTest`, `CommentFinderEndpointTest`, `CommentPublicQueryService*Test`, and `DefaultNotificationTemplateRenderTest`. After adding an explicit stored-root immutability assertion, the 15 HTTP cases and Spotless were rerun.

`openspec validate comment-reply-permalinks --strict` and `git diff --check` also passed.

### Relative URL correction

- A real HTTP regression first reproduced the unwanted origin: a relative post `status.permalink` became `https://configured.example.test/archives/...` when only the site external URL was set.
- Post and SinglePage subject displays now preserve their stored permalink. The reply notification publisher performs external-link conversion at the notification boundary, preserving usable email links and existing subject-link attributes.
- All 70 affected tests passed after the correction: `CommentPermalink*Test`, both built-in subject-provider tests, `CommentNotificationReasonPublisherTest`, and `DefaultNotificationTemplateRenderTest`. The 16 real HTTP cases verify relative links across public and Console responses, plus absolute links produced by the actual post permalink policy with `halo.use-absolute-permalink=true`.
- No frontend code, DTO schema, or generated API output changed in this correction. The initial browser checks below predate it; current URL-form behavior was verified through real HTTP, and notification rendering through publisher/template tests.

### Live Core and Console

Started a separate Halo 2.27.0-SNAPSHOT instance at `127.0.0.1:8090` with a temporary work directory and the current Console dev server. Created a root comment, a guest comment, an admin reply, and a guest reply quoting that admin reply. No external notifier was configured.

| Target | Verified resource name |
| --- | --- |
| Root comment | `e2f531d6-a7eb-4dc4-8d95-00a79305f7ca` |
| Guest comment | `c05b769e-3bc3-4a52-aa4b-2d14bd4f36cd` |
| Quoted reply | `26f55ac8-d76c-4676-b776-72fe9c41d646` |
| New reply | `bb0467d7-cdcd-4975-a2fe-49482e673fe7` |

- Anonymous public detail reads returned the guest comment and new reply with their expected permalinks under `/archives/hello-halo`.
- Newly stored on-site notifications contained matching target links in both HTML and plain text. The reply action used the new reply name, while the quoted text and subject-title URL retained their meanings. HTML escaped `&` as `&amp;` correctly.
- Chromium rendered precise links in `CommentListItem`, `CommentDetailModal`, `ReplyDetailModal`, and the dashboard's pending-comment `CommentItem`. The reply dialog matched the public reply URL; subject-title links still pointed to the Console post editor.
- Each of those four mounted components passed three DOM checks: precise permalink, source-URL fallback, and hidden action with neither URL. Fallback/absence were exercised with temporary component-prop and subject-display fixtures in the browser and restored after each check; normal links used real server responses.
- The isolated browser and servers were stopped after verification.

### Companion boundary

Core verification does not include the companion component's Shadow DOM detail rendering, scrolling, focus, or history acceptance. That work remains in the component repository after Halo 2.27 is published; the adapted plugin/npm component requires Halo 2.27.0 or later. See `docs/developer-guide/comment-permalinks.md` and `design.md` for the handoff.
